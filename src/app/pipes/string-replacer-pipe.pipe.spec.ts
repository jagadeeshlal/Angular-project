import { StringReplacerPipePipe } from './string-replacer-pipe.pipe';

describe('StringReplacerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StringReplacerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
